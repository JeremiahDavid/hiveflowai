# Spreadsheet Engine interpret/propose — container image (Python 3.12 + Node + claude CLI).
# = the "full" Lambda deps plus the vendored agent stack.
-r requirements.txt

# Claude Agent SDK runtime (the `claude` Node CLI is installed separately in the Dockerfile).
claude-agent-sdk>=0.2.0
pydantic>=2.7

# hiveflow-spreadsheet-parser deterministic layer.
pandas>=2.2
python-calamine>=0.2
