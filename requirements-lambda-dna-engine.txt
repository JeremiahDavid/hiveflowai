# DNA Engine UI — catalog, governance, data profile, model mapping, source
# docs browser, and KPI Generator, all as FastAPI/Mangum routes reusing the
# shell's existing hiveflow.dna.web.portal render functions and business
# logic modules unchanged (see hiveflow.dna_engine.web). Same dependency
# footprint as the "reporting" profile it was split out of — this app IS
# that same business logic, not a slimmed-down consumer of it (contrast with
# requirements-lambda-spreadsheet-lab.txt, which deliberately excludes
# pyarrow/pandas) — plus python-multipart, needed for FastAPI's
# `await request.form()` on every write action (data profile overrides,
# model mapping approvals, governance actions, KPI Generator drafting).
httpx>=0.28.0
python-dotenv>=1.0.0
boto3>=1.35.0
pyyaml>=6.0.0
jsonschema>=4.23.0
referencing>=0.28.4,<0.36
typing_extensions>=4.12.0
pyarrow>=18.0.0
werkzeug>=3.1.0
fastapi>=0.115.0
mangum>=0.17.0
python-multipart>=0.0.9
openpyxl>=3.1.5
jinja2>=3.1.0
