# Spreadsheet Engine interpret/propose — plain zip Lambda, no Node/claude CLI.
# = the "full" Lambda deps plus hiveflow-spreadsheet-parser's own stack.
-r requirements.txt

# hiveflow-spreadsheet-parser deterministic layer (grid detection, table extraction).
pandas>=2.2
python-calamine>=0.2
pydantic>=2.7

# hiveflow_core is still imported for plain helpers (e.g. json_default) shared with
# hiveflow_spreadsheet_parser.tools; its own package unconditionally imports
# claude_agent_sdk (see hiveflow_core/__init__.py), so it stays a dependency even
# though nothing here calls into the Claude Agent SDK / claude CLI anymore — this
# is a pure-Python package with no Node.js requirement.
claude-agent-sdk>=0.2.0
